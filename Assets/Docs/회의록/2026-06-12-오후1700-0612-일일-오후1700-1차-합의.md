# 회의록 — 2026-06-12 — 0612 일일 (최초 오후 17:00 · 수정 오후 17:00 · 1차)

| **최초작성** | 2026-06-12 · 오후 17:00 |
| **수정** | 2026-06-12 · 오후 17:00 |
| **수정 차수** | 1차 |
| **주제** | 빌드 직전 · 종료 버튼 · PC 창 모드 · 리사이즈 HUD · 외부 QA 1차 · 시계 UI |
| **버그 기록** | [0612 일일 fixes 1차](../Bug/2026-06-12-오후1700-0612-일일-오후1700-1차-fixes.md) |
| **QA 원본** | [QA_Feedback/HazmatRush/](../QA_Feedback/HazmatRush/) (7건) |

파일명 규칙: [문서-이름-규칙.md](../문서-이름-규칙.md)

---

## 세션 오후 17:00 — 빌드 직전 PC 배포·UI 마감

### 배경

- 브랜치 `260612_빌드직전버전` 기준 PC v1.0 외부 QA 배포 전 마감 작업.
- Popup 코루틴·게임오버 재시작 인벤 유지 등 선행 수정 반영 후, 종료 버튼·창 모드·레이아웃·QA 취합.

### 합의 — 종료하기 버튼

| 위치 | 동작 | 연결 |
|------|------|------|
| **TitleScene** `quitBtn` | 앱 종료 | `SceneLoadUI.QuitButton` → `QuitApplication()` |
| **GameScene** 일시정지 `PauseSet/quitBtn` | 앱 종료 (씬 전환 **금지**) | `GameManager.QuitGame` → `SceneLoadUI.QuitApplication()` |

| 규칙 | 내용 |
|------|------|
| 에디터 | `EditorApplication.isPlaying = false` |
| 빌드 | `Application.Quit()` |
| 일시정지 종료 | 종료 전 `Time.timeScale = 1f` · `pauseSet` 비활성 |
| 타이틀로 돌아가기 | 기존 `toTitleBtn` → `SceneLoadUI.TitleButton` **유지** (종료와 분리) |

### 합의 — PC Windows 빌드·창

| 항목 | 규칙 |
|------|------|
| **창 모드** | Player Settings `fullscreenMode: Windowed` · `resizableWindow: 1` |
| **빌드 런타임** | `AppBootstrap.ForceWindowedOnPcBuild` — Standalone Win 빌드에서 1920×1080 창 모드 |
| **아이콘** | `HazmatRush_Ico.png` · `PlayerIconBuildPreprocessor` (Windows 빌드 전 적용) |
| **커서** | Player Settings Default Cursor 연결만 유지 — **표시 이슈 추가 작업 보류** (팀 합의) |
| **배포** | `Hazmat Rush.exe` + `Hazmat Rush_Data` 통째 zip |

### 합의 — 창 리사이즈·HUD 레이아웃

| 항목 | 규칙 |
|------|------|
| **게임 카메라** | `CameraAspectFit` — cover (넓은 창 위·아래 파란 여백 방지) |
| **Canvas Scaler** | 5씬 `Match Width Or Height = 0` (Width 기준) |
| **메뉴 배경** | `CanvasBackgroundCover` — Title/Splash/Loading/Intro `Bg`·`TitleImage` cover |

### 구현 상태

- ✅ 종료 버튼·`QuitGame` / `QuitApplication`
- ✅ PC 창 모드·리사이즈 레이아웃 1차
- ✅ 아이콘 빌드 전처리
- 🟡 커서 인게임 표시 — **보류**
- 🟡 시계 UI 중앙 이동 — **다음 작업** (본인 담당)

---

## 세션 오후 17:00 — 외부 QA 1차 피드백 (7명)

### 배경

- PC (.exe) v1.0 · Formal Guide 10항목 · `Assets/Docs/QA_Feedback/HazmatRush/` 에 `.md` 7건 수집.

### QA 10항목 요약

| 항목 | 이슈 |
|------|------|
| QA-02 화면 이동 흐름 | 1명 판단 어려움 |
| QA-07 맞는/틀린 도구 | 1명 — 정답 도구인데 방호복 HP 감소가 의도인지 불명 |
| QA-09 클리어·게임오버 | 1명 문제 있음 (상세 미기재) |
| 나머지 | 대체로 정상 |

**전체:** 7/7 「전반적으로 정상」 · 구체적 재현 버그 리포트 **없음**

### 반복 피드백 (우선순위)

| 순위 | 주제 | 건수 | 대표 의견 |
|------|------|------|-----------|
| 1 | **튜토리얼·도구 설명** | 5+ | 스캐너 역할·Z/X·인벤 효과·접촉 해제 시 HP 초기화 설명 부족 |
| 2 | **이동 잠금·팝업 답답함** | 3 | 오염 경고 시 정지·틀린 도구 이동 불가·짧은 팝업 중 이동 불가 |
| 3 | **HUD** | 2 | 시계와 Item Guide 겹침 → **시계를 화면 중앙(목표 아래)으로** (팀 반영 예정) |
| 4 | **회복 밸런스** | 1 | 회복템 회복량 부족 |
| 5 | **조작 편의** | 1 | 마우스·숫자키 단축 희망 |
| 6 | **배포** | 1 | exe 파일명·실행 대상 혼동 |

### 합의 — 1차 대응 (빌드 직후)

| 항목 | 결정 |
|------|------|
| **시계 UI** | 개발자가 **화면 가운데(현재 목표 아래)** 로 이동 — QA 장상욱 의견 반영 |
| **튜토리얼·스캐너·인벤 설명** | 2차 빌드 전 기획·문구 검토 (본 회차 코드 변경 없음) |
| **회복량·이동 잠금·조작 단축** | 백로그 — 합의 후 별도 |
| **QA-09** | Checked 응답자 재확인 또는 내부 재현 |

### 금지·주의

| 항목 | 내용 |
|------|------|
| 일시정지 종료 | `TitleButton` / 씬 로드로 **대체하지 않음** — 반드시 `QuitGame` |
| 접촉·이동 | QA 피드백 대응 시 [회의록 §4](../회의록/2026-06-05-시각미상-플레이어-오염원-로직-검토.md) 표준 모델 유지 |
| 커서 | Player Settings만 유지 — 런타임 `SetCursor` 임의 추가 금지 (요청 시만) |

---

## 변경 이력

| 시각 | 내용 |
|------|------|
| 오후 17:00 | 빌드 직전 합의 1차 — 종료·창 모드·레이아웃·QA 7건 취합·시계 UI 후속 |
