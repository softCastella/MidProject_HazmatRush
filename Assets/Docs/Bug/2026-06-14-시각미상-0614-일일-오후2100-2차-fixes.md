# Bug — 2026-06-14 — 0614 일일 (최초 시각미상 · 수정 2026-06-14 오후 21:00 · 2차)

| **최초작성** | 2026-06-14 · 시각미상 |
| **수정** | 2026-06-14 · 오후 21:00 |
| **수정 차수** | 2차 |
| **관련 합의** | [0614 일일 합의 2차](../회의록/2026-06-14-시각미상-0614-일일-오후2100-2차-합의.md) |
| **선행** | [0612 일일 fixes 1차](2026-06-12-오후1700-0612-일일-오후1700-1차-fixes.md) |

---

## 1. GameScene 타이머 숫자 미감소

| 항목 | 내용 |
|------|------|
| **증상** | GameScene Play 시 HUD 타이머가 줄지 않음. |
| **원인** | `Timer.Start()`가 `isRunning = false`로 리셋 · `StageManager`가 `StartCountdown()` 미호출 · 실행 순서 충돌. |
| **해결** | `Timer` `[DefaultExecutionOrder(100)]` · `Start()`에서 `isRunning` 강제 false 제거 · `StageManager.LoadStage` → `SetStartTime` + `StartCountdown()`. |
| **파일** | `Timer.cs`, `StageManager.cs` |

### Play 체크리스트

- [ ] `GameScene` Play → 스테이지 로드 직후 타이머 감소

---

## 2. 타이머 경고 SFX 중복·조기 종료

| 항목 | 내용 |
|------|------|
| **증상** | 경고음이 2초 부근에서 끊김 · 오염 경고 후 **처음부터 다시** 재생. |
| **원인** | `StopCountdown()`이 SFX까지 Stop+리셋 · `isRunning == false`일 때 `UpdateWarningSfx`가 매 프레임 `StopWarningSfx()` 호출. |
| **시도** | `warningSfxStartSeconds = 20` (클립 10→0과 불일치) — **16초로 조정**. |
| **해결** | `PauseCountdown()` / `ResumeCountdown()` — SFX Pause·UnPause · `warningSfxStarted`로 **1회만** 시작 · 임계값 초과·0초에서만 Stop. |
| **파일** | `Timer.cs`, `PollutantManager.cs`, `GameScene.unity` (`countdownSFX.ogg` 연결) |

### Play 체크리스트

- [ ] 17초→16초 진입 시 SFX **1회** 시작
- [ ] 오염 경고 중 멈춤 → 등장 후 **이어 재생** (처음부터 X)
- [ ] 0초 SFX 정지 + 타임오버
- [ ] HUD 16→0 **빨간 숫자**

---

## 3. 창 리사이즈 시 UI·배경 스케일 불일치

| 항목 | 내용 |
|------|------|
| **증상** | 가로 줄이면 HUD만 위·아래에 붙고 월드는 잘림 · UI는 작아지는데 배경은 cover로 확대. |
| **원인** | 0612 `Match Height` 전환 + `CameraAspectFit` cover + `CanvasBackgroundCover`가 카메라 검정 강제. 1280×**1080** 설정과 16:9 설계(1920×1080) 불일치. |
| **해결** | `CanvasBackgroundCover` — Match Width + 세로 긴 화면 reference 높이 동적 확장 · `CameraAspectFit` — ortho 고정 + 배경 **contain** · `InventoryContainer` 우측 앵커 · `defaultScreenHeight: 720`. |
| **파일** | `CanvasBackgroundCover.cs`, `CameraAspectFit.cs`, `GameScene.unity`, `ProjectSettings.asset` |

### Play 체크리스트

- [ ] **1280×720** 빌드/Play → UI·배경 비율 맞음 · 하단 Item/Inventory 붙음
- [ ] (알려진 한계) 비 16:9로 창 늘리면 레이아웃 깨질 수 있음 — 발표는 1280×720 고정 권장

---

## 4. 스플래시 배경 검정 + 로고 흰 박스

| 항목 | 내용 |
|------|------|
| **증상** | 스플래시가 예전 **카메라 흰색** 전체가 아니라 검정 + 가운데 흰 사각형 로고. |
| **원인** | `CanvasBackgroundCover.Apply()`가 `Camera.main.backgroundColor = Color.black` 실행 (Splash `Main Camera`는 흰색 설정). |
| **해결** | 해당 **강제 검정 코드 제거** — 씬 Inspector 배경색 유지. |
| **파일** | `CanvasBackgroundCover.cs` |

### Play 체크리스트

- [ ] `AppScene` Play → 스플래시 **흰 배경** + 로고 페이드

---

## 5. 빌드 첫 실행에 타이틀 이어하기 표시

| 항목 | 내용 |
|------|------|
| **증상** | 새 빌드인데 타이틀에 **이어하기** 버튼 노출. |
| **원인** | `GameSaveManager`가 **`Documents/HazmatRush_Save`** 를 참조 — 예전 에디터·빌드 플레이 저장이 PC에 남음. |
| **해결** | **빌드**: `[exe 폴더]/Save/gamesave.json` · **에디터**: Documents 유지 · `SceneLoadUI` / `HasSave()` 동일 API. |
| **파일** | `GameSaveManager.cs`, `SceneLoadManager.cs` (`StartButton` → `EnsureSaveDirectory`) |

### Play 체크리스트

- [ ] 빌드 exe 옆 `Save` 없음 → 타이틀 **시작하기만**
- [ ] 스테이지 클리어 후 → `Save/gamesave.json` 생성 → **이어하기** 표시
- [ ] 에디터 Play → Documents 경로 (기존과 동일)

---

## 6. 사망 Die 애니 SFX 추가

| 항목 | 내용 |
|------|------|
| **요청** | Die 애니와 함께 `dieSplattSFX` 재생 (게임오버 패널음과 별도). |
| **해결** | `AudioManager.dieSplattClip` · `PlayDieSplattSfx()` · `Player.StartDeathSequence()`에서 호출 · 씬 AudioManager 연결. |
| **파일** | `AudioManager.cs`, `Player.cs`, App/Title/Splash/Game 씬 |

### Play 체크리스트

- [ ] 방호복 0 → Die 애니 + **dieSplatt** · 2초 후 패널 + **game-over**

---

## 7. 변경 파일 요약

| 파일 | 내용 |
|------|------|
| `Timer.cs` | SFX · Pause/Resume · 빨간색 · 16초 |
| `StageManager.cs` | `StartCountdown` |
| `PollutantManager.cs` | `PauseCountdown` / `ResumeCountdown` |
| `CanvasBackgroundCover.cs` | reference 동적 · 검정 강제 제거 |
| `CameraAspectFit.cs` | contain |
| `GameSaveManager.cs` | 빌드 `Save/` |
| `SceneLoadManager.cs` | `EnsureSaveDirectory` |
| `AudioManager.cs` · `Player.cs` | dieSplatt |
| `GameScene.unity` | Timer · Inventory · HUD cover |
| `ProjectSettings.asset` | 1280×720 |

---

## 8. 전체 Play 체크리스트 (0614 · 2차)

- [ ] App → 스플래시 **흰 배경** → 타이틀 (저장 없으면 Start만)
- [ ] GameScene 타이머·16초 SFX·빨간 숫자·오염 경고 Pause/Resume
- [ ] 클리어 → exe `Save/gamesave.json` → 타이틀 이어하기
- [ ] 사망 dieSplatt + 게임오버 SFX
- [ ] 1280×720 빌드 HUD·배경 정렬
