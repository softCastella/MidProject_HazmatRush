# Bug — 2026-06-11 — 0611 일일 (최초 오후 14:30 · 수정 오후 21:00 · 2차)

| **최초작성** | 2026-06-11 · 오후 14:30 |
| **수정** | 2026-06-11 · 오후 21:00 |
| **수정 차수** | 2차 |
| **관련 합의** | [0611 일일 합의 2차](../회의록/2026-06-11-오후1430-0611-일일-오후2100-2차-합의.md) |
| **선행** | [2026-06-06 씬전환](2026-06-06-시각미상-씬전환-스플래시-인트로-fixes.md) · [2026-06-05 스플래시·오디오](2026-06-05-시각미상-스플래시-오디오-fixes.md) |

피드백 대응·설정 반영·QA 문서 기록입니다.

---

## 1. 타이틀 빌드 버전 미표시

| 항목 | 내용 |
|------|------|
| **증상** | Player Settings **Version**이 화면에 없음. QA·빌드 구분 어려움. |
| **원인** | `Application.version` 표시 UI·스크립트 없음. |
| **해결** | `VersionText.cs` — `Awake`에서 `"v" + Application.version` → TMP. `TitleScene` `TItle_HUD_Canvas/VersionTxt` 우하단(앵커 우하, 14pt). Raycast Target OFF. |
| **파일** | `Scripts/UI/VersionText.cs`, `TitleScene.unity` |

### Play 체크리스트

- [ ] `TitleScene` 또는 `AppScene` Play → 우하단 `v1.0` (Player Settings Version과 일치)
- [ ] Project Settings Version 변경 후 Play → 표시 갱신

---

## 2. 씬 전환 페이드 색 불일치 (피드백)

| 항목 | 내용 |
|------|------|
| **증상** | 타이틀→인트로 등 구간이 **흰** 페이드, 다른 구간은 **검정** — 전환마다 색이 달라 보임. |
| **원인** | [0606 씬전환](../Bug/2026-06-06-시각미상-씬전환-스플래시-인트로-fixes.md)에서 타이틀→인트로 **흰** 체인 합의. 스플래시→타이틀은 검은 암전 **제거** 상태. |
| **합의** | 스플래시 **로고 페이드**(흰 배경)만 예외. **나머지 씬 전환은 검정** 통일. |
| **해결** | 아래 §3~§5. |

---

## 3. 스플래시 → 타이틀 검정 암전 추가

| 항목 | 내용 |
|------|------|
| **요청** | 스플래시 종료 후 타이틀 전 **검정** 페이드. |
| **해결** | `SplashController` — 로고 페이드아웃·로드 대기 후 `FadeOverlay`(검정) 0→1, `fadeToBlackDuration` 기본 **0.35s**, `allowSceneActivation`. |
| **파일** | `SplashController.cs`, `SplashScene.unity` |

### Play 체크리스트

- [ ] `AppScene` Play → 로고 사라진 뒤 **짧은 검은 화면** → 타이틀
- [ ] 스플래시 로고 **페이드인**은 여전히 **흰 배경**

---

## 4. 타이틀 → 인트로 흰 페이드 → 검정

| 항목 | 내용 |
|------|------|
| **증상** | Start 시 **흰** 화면 암전·인트로 **흰** 페이드인. |
| **해결** | `TitleSceneFade` — `FadeOverlay`·카메라 **검정**. `AutoScrollIntro` — `fadeInFromTitle` 시 오버레이 **검정** 불투명 시작 → 0.8s 페이드인. |
| **파일** | `TitleSceneFade.cs`, `AutoScrollIntro.cs` |

### Play 체크리스트

- [ ] 타이틀 Start → **검정** 0.25s → 인트로 **검정**에서 페이드인 (흰 화면 없음)
- [ ] 인트로 종료 → 로딩 **검정** 0.5s (기존과 동일)

---

## 5. 변경 파일 요약 (코드 · 1차)

| 파일 | 내용 |
|------|------|
| `VersionText.cs` | 타이틀 버전 TMP |
| `TitleScene.unity` | `VersionTxt` |
| `SplashController.cs` | 스플래시→타이틀 검정 암전 |
| `SplashScene.unity` | `fadeToBlackDuration` |
| `TitleSceneFade.cs` | 타이틀→인트로 검정 |
| `AutoScrollIntro.cs` | 인트로 진입 검정 페이드인 |

---

## 6. 전체 Play 체크리스트 (0611 · 1차)

- [ ] App → 스플래시(흰 로고) → 검정 → 타이틀 `v1.0`
- [ ] Start → 검정 → 인트로 → 검정 → 로딩 → 게임
- [ ] Continue → 인트로 생략, 로딩 검정 (기존)

---

## 7. QA 문서 `_Data` 폴더명 표기 오류 (문서)

| 항목 | 내용 |
|------|------|
| **증상** | QA HTML에 exe 옆 **`_Data`** 폴더라고 적혀 있음. 테스터가 해당 이름 폴더를 찾지 못할 수 있음. |
| **원인** | Unity 관례(`{productName}_Data`)를 축약해 `_Data`로만 표기. 실제 `productName`은 **`Hazmat Rush`**. |
| **확인** | Windows 빌드 시 산출물: `Hazmat Rush.exe` + **`Hazmat Rush_Data`** (공백 포함). exe만 옮기면 실행 실패 — **원칙은 맞으나 폴더명이 틀림**. |
| **해결** | 🟡 문구 보정 대기 — `Hazmat Rush_Data` 또는 「exe와 같은 폴더의 `(게임이름)_Data`」로 통일 |
| **파일** | `Docs/QA/qa-B.html`, `qa-tc-2026-06-11.html`, `qa-tc-2026-06-11_A.html`, `qa-tc-2026-06-11_B.html`, `qa-tc-2026-06-12-formal.html` |

### 체크리스트

- [ ] QA HTML 배포 안내 문구가 `Hazmat Rush_Data`와 일치
- [ ] zip 배포 시 exe + Data 폴더 + MonoBleedingEdge 통째로 포함

---

## 8. 외부 QA Formal 안내서 추가 (문서 · 2차)

| 항목 | 내용 |
|------|------|
| **배경** | Ratchemy Formal Guide v4 형식 — 외부 비전문가 **기능 QA** 10문항 필요. |
| **해결** | `Docs/QA/qa-B.html` 작성 — 게임 개요·진행 순서·10 QA(정상/문제/판단 어려움)·피드백·버그 리포트·`.md` 저장. |
| **관련** | [0611 합의 2차 §21:00](../회의록/2026-06-11-오후1430-0611-일일-오후2100-2차-합의.md) |

### 체크리스트

- [ ] `qa-B.html` 브라우저에서 10문항·md 저장 동작
- [ ] 외부 QA와 `qa-tc-2026-12-formal`(느낌 설문) 역할 구분 안내

---

## 9. PC 빌드 전 확인 (미해결 · 씬)

| 항목 | 내용 |
|------|------|
| **증상** | `GameScene` `RecoveryItemManager.testAlwaysDrop`이 **ON**일 수 있음 — 빌드 시 회복 아이템 100% 드랍. |
| **원인** | 개발·테스트용 플래그. [0608 회복 드랍 합의](../회의록/2026-06-08-시각미상-회복아이템-드랍-연출-합의.md) — **빌드 전 OFF**. |
| **해결** | 🟡 빌드 직전 Inspector에서 OFF 확인 |
| **파일** | `GameScene.unity`, `RecoveryItemManager.cs` |

### Play 체크리스트

- [ ] `testAlwaysDrop` OFF 상태에서 빌드
- [ ] 빌드 산출물 `Hazmat Rush.exe` + `Hazmat Rush_Data` 실행
- [ ] 타이틀 `v1.0` · AppScene 전체 플로우

---

*2026-06-11 작성 · 21:00 2차 취합*
